class JstTimer {
  constructor(name, timeFirst, timeSecond) {
    this.name = name;
    // Time in ms
    this.timeFirst = timeFirst;
    this.timeSecond = timeSecond;
  }
}

export { JstTimer };
